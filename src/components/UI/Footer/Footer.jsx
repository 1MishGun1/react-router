import Styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={Styles.footer}>
      <h2 className={Styles.footer__content}>This is footer</h2>
    </footer>
  );
};

export default Footer;
